{
  "name": "wedding-landing",
  "version": "1.0.0",
  "description": "React + ASP.NET Core fullstack app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"dotnet watch run --project server/src/WeddingLanding.Web\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix client",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
 